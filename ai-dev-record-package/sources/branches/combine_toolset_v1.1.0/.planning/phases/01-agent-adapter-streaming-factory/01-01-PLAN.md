---
phase: 01-agent-adapter-streaming-factory
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/adapters/types.py
  - backend/app/adapters/base_adapter.py
  - backend/app/adapters/factory.py
  - backend/app/adapters/mock_adapter.py
  - backend/app/adapters/normalizers.py
  - backend/app/adapters/codex_adapter.py
  - backend/app/adapters/claude_adapter.py
  - backend/app/adapters/opencode_adapter.py
  - backend/app/adapters/__init__.py
  - backend/app/services/orchestrator_service.py
  - backend/app/services/message_service.py
  - frontend/src/views/Dashboard.vue
  - frontend/src/store/modules/message.js
  - frontend/src/components/AgentEditForm/index.vue
  - frontend/vue.config.js
  - backend/app/controllers/message_controller.py
  - backend/app/schemas/agent_schema.py
  - backend/tests/test_adapter_factory.py
  - backend/tests/test_adapter_normalizers.py
  - backend/tests/test_orchestrator_adapter_stream.py
  - backend/tests/test_mock_streaming_adapter.py
  - docs/tech/modules/E_agent_adapter.md
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-06
  - REQ-07
  - REQ-08
user_setup:
  - "Optional: set AGENT_WORKSPACE_ROOT to an isolated workspace root; if omitted, the adapter uses the WeAgent project root."
must_haves:
  truths:
    - "D-01: All adapters expose stream(request) as the canonical event-producing method."
    - "D-02: send_prompt remains available as a compatibility aggregator during migration."
    - "D-03: Provider raw output is normalized before orchestrator or SSE consumption."
    - "D-04: Codex MVP path uses codex exec JSONL streaming."
    - "D-05: Claude MVP path uses claude print mode with stream-json and partial messages."
    - "D-06: SDK integrations remain documented as a follow-up, not required for MVP."
    - "D-07: A deterministic mock adapter exists and is registered."
    - "D-08: CLI and parse failures surface as agent.failed events."
    - "D-09: Orchestrator accumulates deltas and persists final agent text."
    - "D-10: Streaming events are broadcast before final persistence."
    - "D-11: REST message API plus SSE is the primary user-facing stream path."
    - "D-12: Frontend handles message.delta, message.completed, and agent.failed."
    - "D-13: Workspace resolution uses AGENT_WORKSPACE_ROOT with WeAgent project-root fallback."
  artifacts:
    - path: backend/app/adapters/factory.py
      provides: Adapter factory and provider registry.
    - path: backend/app/adapters/mock_adapter.py
      provides: Deterministic mock streaming fallback.
    - path: backend/app/adapters/normalizers.py
      provides: Codex and Claude raw event normalization.
    - path: frontend/src/views/Dashboard.vue
      provides: EventSource listeners for streaming agent events.
  key_links:
    - from: backend/app/services/orchestrator_service.py
      to: backend/app/adapters/factory.py
      via: AgentAdapterFactory.create
      pattern: AgentAdapterFactory\.create
    - from: frontend/src/views/Dashboard.vue
      to: backend/app/controllers/message_controller.py
      via: EventSource SSE stream
      pattern: EventSource
---

# Phase 1 Plan: Agent Adapter Streaming Factory

## Objective

Build a factory-created streaming adapter layer for Codex and Claude Code while preserving a stable mock fallback.

## Architecture

The backend introduces request/event helpers, provider-specific adapters, normalizers, and an adapter factory. The orchestrator consumes normalized events, broadcasts deltas, and persists the final agent message after completion.

## Decision Coverage

- D-01: Covered by Task 1.
- D-02: Covered by Task 1.
- D-03: Covered by Task 3 and Task 5.
- D-04: Covered by Task 4.
- D-05: Covered by Task 4.
- D-06: Covered by Task 4 and Task 7 documentation.
- D-13: Covered by Task 1 and Task 5.
- D-07: Covered by Task 2.
- D-08: Covered by Task 3 and Task 4.
- D-09: Covered by Task 5.
- D-10: Covered by Task 5.
- D-11: Covered by Task 5 and Task 6.
- D-12: Covered by Task 6.

## Tasks

### Task 1: Define Shared Adapter Contract

- [x] Create `backend/app/adapters/types.py` with `AgentRequest`, `AdapterHealth`, `AgentEvent`, `utc_now_iso()`, and `make_event()`.
- [x] Create a shared workspace resolver that reads `AGENT_WORKSPACE_ROOT` and falls back to the `WeAgent` project root.
- [x] Update `backend/app/adapters/base_adapter.py` so `stream(request)` is the abstract method.
- [x] Keep `send_prompt(prompt, context=None)` as a compatibility aggregator over `message.delta` and `message.completed`.
- [x] Verify with `python -m py_compile backend/app/adapters/types.py backend/app/adapters/base_adapter.py`.

### Task 2: Add Factory Registry

- [x] Create `backend/app/adapters/factory.py` with `AgentAdapterFactory.register()`, `create()`, and `providers()`.
- [x] Create `backend/app/adapters/mock_adapter.py` with a deterministic streaming sequence: `agent.started`, several `message.delta` events, and `message.completed`.
- [x] Update `backend/app/adapters/__init__.py` to register `mock`, `claude`, `codex`, and `opencode`.
- [x] Keep `get_adapter(adapter_name)` available for migration compatibility.
- [x] Add focused factory tests under `backend/tests/test_adapter_factory.py`.
- [x] Add mock stream tests under `backend/tests/test_mock_streaming_adapter.py`.

### Task 3: Add Provider Normalizers

- [x] Create `backend/app/adapters/normalizers.py`.
- [x] Implement `normalize_codex_event(raw, request)` returning zero or more normalized events.
- [x] Implement `normalize_claude_event(raw, request)` returning zero or more normalized events.
- [x] Cover `message.delta`, `message.completed`, `tool.started`, `tool.completed`, `artifact.created`, and `agent.failed`.
- [x] Add tests under `backend/tests/test_adapter_normalizers.py` with representative fake raw events.

### Task 4: Implement CLI Streaming Adapters

- [x] Update `backend/app/adapters/codex_adapter.py` to run `codex exec --json --cd <workspace> --sandbox workspace-write --skip-git-repo-check <prompt>`.
- [x] Update `backend/app/adapters/claude_adapter.py` to run `claude -p --output-format stream-json --verbose --include-partial-messages --permission-mode plan <prompt>`.
- [x] Parse stdout line by line as JSON.
- [x] Emit `agent.failed` on missing CLI, invalid startup, or non-zero exit.
- [x] Test adapters by monkeypatching subprocess creation; do not call real Claude or Codex in unit tests.

### Task 5: Integrate Orchestrator Streaming

Status: backend integration completed on 2026-05-24 after real Claude/Codex adapter smoke tests. Frontend-specific streaming rendering remains deferred.

- [x] Replace direct adapter class construction in `backend/app/services/orchestrator_service.py` with `AgentAdapterFactory.create(agent.adapter_name)`.
- [x] Build an `AgentRequest` from conversation, user message, agent profile, and the shared workspace resolver from D-13.
- [x] Broadcast normalized events as they arrive through the existing conversation stream path.
- [x] Accumulate deltas and persist one final agent message on `message.completed`.
- [x] Preserve logging and visible `agent.failed` events for adapter exceptions.
- [x] Route the REST `message_service.send_message(... sender_type='user')` path through the orchestrator instead of the existing `_mock_agent_response` demo path.
- [x] Keep Socket.IO compatible by avoiding a second independent agent invocation path.

### Task 6: Add Minimal Frontend SSE Streaming Support

Status: completed on 2026-05-24 after backend orchestrator integration.

- [x] Update `frontend/src/views/Dashboard.vue` so `EventSource` listens for `message.delta`, `message.completed`, and `agent.failed`.
- [x] Add Vuex mutations in `frontend/src/store/modules/message.js` to create or update a temporary streaming agent message by `runId` and `agentId`.
- [x] Keep the current default `onmessage` handler for persisted catch-up messages.
- [x] On `message.completed`, replace or finalize the temporary streaming message with final text while preserving later persisted-message de-duplication.
- [x] On `agent.failed`, show a failed agent bubble and stop the responding indicator.
- [x] Update backend SSE formatting so normalized agent events use named SSE events while persisted DB messages keep the default data event.

### Task 7: Verify And Document

- [x] Run `python -m compileall backend/app/adapters backend/app/services`.
- [x] Run backend tests with the repository-approved test command. If no test framework is configured, use `python -m unittest discover backend/tests`.
- [x] Run a frontend syntax/build check after modifying `frontend/src/views/Dashboard.vue` and `frontend/src/store/modules/message.js`.
- [x] Update `docs/tech/modules/E_agent_adapter.md` if implementation details diverge from the current design document.
- [x] Commit and push completed increments:
  - `4dd020d feat: add streaming agent adapters`
  - `8f0ca8c fix: harden cli streaming adapters`
  - `034ea0c fix: support real codex streaming output`
  - `d17a1bf feat: route orchestrator through agent adapters`
  - `0dddf76 feat: stream adapter events to frontend`
  - `07b6e90 docs: add adapter frontend test report`

## Acceptance Criteria

- Orchestrator can invoke Codex, Claude, or Mock-like adapters through one factory.
- Adapter streams emit normalized `AgentEvent` dictionaries.
- Streaming deltas can be broadcast and rendered before final message persistence.
- Failures surface as `agent.failed` events.
- Tests do not require real Claude or Codex network/API calls.
