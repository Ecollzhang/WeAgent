"""
sandbox.api — Optional Flask Blueprint for testing the Sandbox module.

To use::

    from app.sandbox.api.routes import sandbox_bp
    app.register_blueprint(sandbox_bp, url_prefix='/api/sandbox')
"""
