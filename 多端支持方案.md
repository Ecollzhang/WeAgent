# WeAgent 多端支持方案

## 目标

在不改动现有 Web 前端页面代码的前提下，为 WeAgent 增加桌面端和 Android 端支持。

当前阶段只处理两类核心界面：

- 登录 / 注册界面
- 会话界面

桌面端只连接外部后端，不内置、不自动启动 Python 后端。后端地址由用户自行配置。移动端首期只支持 Android，并且需要支持当前局域网测试，未来可切换到公网后端。

## 方案选择

### 桌面端：Electron

桌面端选择 Electron。

原因：

- Vue 2 + Vue CLI 项目接入成本低。
- Windows 开发、调试、打包链路成熟。
- 适合当前“只连接外部后端”的需求。
- 后续如果需要桌面原生能力，例如文件选择、系统通知、托盘、下载管理，可以通过 Electron 主进程逐步补充。

本阶段不选择 Tauri 作为主线。Tauri 体积更小，但需要 Rust 工具链，且当前目标是尽快建立可运行的桌面端链路，Electron 更稳。

### 手机端：Capacitor Android

Android 端选择 Capacitor。

原因：

- 比 Cordova 更现代，维护体验更好。
- 可以直接承载 WebView 页面，同时保留调用原生能力的空间。
- Android 本地调试、真机调试、后续上架链路都比较清晰。

本阶段不选择 Cordova 作为主线。Cordova 可作为兼容备选，但不建议同时维护两套移动容器。

## 总体架构

现有 Web 前端保持不动：

```text
frontend/                 # 现有 Web 前端，暂不改页面和功能
backend/                  # 现有后端，未来部署公网
clients/
  desktop/                # Electron 桌面端
  android/                # Capacitor Android 端
packages/
  shared-api/             # 可选：后续抽取公共 API / socket 客户端
docs/
  multi-platform.md       # 可选：后续文档归档
```

当前不建议直接在 `frontend/src/views` 中改登录、注册、会话页面来适配多端。桌面端和 Android 端可以先建立独立入口，复用接口协议和部分组件思路，但页面结构允许单独调整。

## 为什么不直接改现有 Web 前端

现有 Web 前端已经依赖 Web 开发代理模式：

- `vue.config.js` 中开发代理 `/api`、`/uploads`、`/socket.io` 到后端。
- `frontend/src/api/axios.js` 使用 `baseURL: '/api'`。
- `frontend/src/api/sandbox.js` 中的文件预览和下载 URL 直接拼接 `/api/sandbox/...`。
- `frontend/src/utils/socket.js` 使用 `io('/')`。
- `frontend/src/router/index.js` 使用 `history` 路由。

这些设计适合浏览器 Web 端，但在 Electron、Android WebView 中需要重新处理后端地址、路由 fallback、文件 URL、socket 地址。

为了避免影响现有 Web，本阶段采用“新增多端客户端”的方式，而不是在 Web 页面中做大量条件判断。

## 后端连接策略

多端客户端统一使用“用户可配置后端地址”。

配置项：

```text
WEAGENT_SERVER_URL=http://192.168.1.100:5002
```

客户端内部派生：

```text
API_BASE_URL=http://192.168.1.100:5002/api
SOCKET_BASE_URL=http://192.168.1.100:5002
UPLOADS_BASE_URL=http://192.168.1.100:5002/uploads
```

### 局域网测试

后端目前监听：

```text
0.0.0.0:5002
```

局域网测试时：

1. 后端机器和 Android 真机 / 桌面端处于同一局域网。
2. 后端机器防火墙允许访问 `5002` 端口。
3. 客户端配置后端机器的局域网 IP，例如：

```text
http://192.168.1.100:5002
```

Android 真机不要配置：

```text
http://localhost:5002
```

因为手机里的 `localhost` 指的是手机自身。

Android 模拟器可使用：

```text
http://10.0.2.2:5002
```

### 公网部署

未来后端部署公网后，客户端只需要把服务地址改成：

```text
https://api.example.com
```

生产环境建议使用 HTTPS，否则 Android 新版本和部分 WebView 场景可能需要额外配置明文 HTTP 访问权限。

## 桌面端设计

### 目录

```text
clients/desktop/
  package.json
  electron/
    main.js
    preload.js
  src/
    main.js
    router.js
    api/
      client.js
      auth.js
      conversation.js
      socket.js
    views/
      Login.vue
      Register.vue
      Conversation.vue
    components/
      ServerConfigDialog.vue
```

### 核心功能

桌面端首期支持：

- 配置后端地址
- 登录
- 注册
- 保存登录 token
- 拉取会话列表
- 进入会话
- 发送消息
- 接收 socket 实时消息
- 退出登录

暂不处理：

- sandbox 文件树
- 文件预览
- 服务预览
- 复杂 artifact 渲染
- 本地后端启动
- 系统托盘
- 自动更新

### 后端地址配置

桌面端提供一个轻量配置入口：

- 首次启动时如果没有配置后端地址，显示服务地址配置界面。
- 登录页保留“服务器设置”入口。
- 配置保存到 Electron 本地存储，例如 `app.getPath('userData')/config.json`。

示例配置：

```json
{
  "serverUrl": "http://192.168.1.100:5002"
}
```

渲染进程不直接访问 Node API，通过 `preload.js` 暴露安全方法：

```js
window.weagentConfig.getServerUrl()
window.weagentConfig.setServerUrl(url)
```

### Electron 安全策略

建议配置：

```js
webPreferences: {
  contextIsolation: true,
  nodeIntegration: false,
  preload: path.join(__dirname, 'preload.js')
}
```

这样页面不能直接访问 Node 环境，后续扩展桌面能力也更安全。

## Android 端设计

### 目录

```text
clients/android/
  package.json
  capacitor.config.ts
  android/
  src/
    main.js
    router.js
    api/
      client.js
      auth.js
      conversation.js
      socket.js
    views/
      Login.vue
      Register.vue
      Conversation.vue
    components/
      ServerConfig.vue
```

### 核心功能

Android 首期支持：

- 配置后端地址
- 登录
- 注册
- 保存登录 token
- 拉取会话列表
- 进入会话
- 发送消息
- 接收 socket 实时消息
- 退出登录

暂不处理：

- iOS
- 推送通知
- 相机 / GPS
- 文件系统深度集成
- Android 后台长连接保活
- 应用上架配置

### Android 明文 HTTP

局域网测试通常使用 HTTP，例如：

```text
http://192.168.1.100:5002
```

因此 Android 调试阶段需要允许明文 HTTP。

可在 Capacitor 配置或 Android 原生配置中启用：

```xml
android:usesCleartextTraffic="true"
```

生产环境切 HTTPS 后，应关闭或收紧明文 HTTP 配置。

### Android 后端地址输入

Android 首次启动流程：

1. 检查本地是否保存 `serverUrl`。
2. 如果没有，进入服务器配置页。
3. 用户输入局域网后端地址。
4. 点击“测试连接”，请求：

```text
GET {serverUrl}/api/health
```

5. 测试成功后保存地址并进入登录页。

## 登录 / 注册转换方案

### Web 当前逻辑

现有 Web 登录注册主要依赖：

- `POST /api/auth/login`
- `POST /api/auth/register`
- `GET /api/auth/profile`
- `access_token`
- `refresh_token`
- `localStorage`

这些协议可以被桌面端和 Android 端复用。

### 多端实现

多端客户端新建独立页面：

```text
Login.vue
Register.vue
```

保留与 Web 相同的接口协议，但 UI 可以针对桌面和手机分别调整。

桌面端：

- 居中登录面板
- 保留服务器设置入口
- 支持记住服务器地址
- 登录成功进入会话界面

Android 端：

- 移动端单列布局
- 输入框高度和按钮触控区域适配手机
- 首次启动先配置服务器
- 登录成功进入会话界面

### Token 存储

首期可以使用 WebView / Electron 的本地存储：

```text
access_token
refresh_token
user
```

后续增强：

- Electron 使用安全存储或系统凭据管理。
- Android 使用 Capacitor Preferences 或安全存储插件。

## 会话界面转换方案

### Web 当前逻辑

现有会话页包含大量功能：

- 会话列表
- Agent 选择
- 消息收发
- socket 实时更新
- sandbox 文件树
- 文件预览
- 服务预览
- artifact 渲染
- 附件上传

多端首期不完整照搬 Web Dashboard，只保留核心会话能力。

### 多端首期会话界面

桌面端和 Android 端共用一套简化信息架构：

```text
会话列表
  ├─ 新建会话
  ├─ 会话搜索，可选
  └─ 最近会话

会话详情
  ├─ 消息列表
  ├─ Agent 消息状态
  ├─ 输入框
  ├─ 发送按钮
  └─ socket 实时更新
```

首期接口：

```text
GET    /api/conversations
POST   /api/conversations
GET    /api/conversations/:id
DELETE /api/conversations/:id
GET    /api/messages/conversation/:conversationId
POST   /api/messages
```

socket 事件：

```text
connect
join
leave
new_message
agent_typing
agent_typing_status
sandbox_event
```

### 桌面端会话布局

桌面端建议双栏：

```text
┌──────────────┬──────────────────────────┐
│ 会话列表      │ 消息区域                  │
│              │                          │
│              │                          │
│              │ 输入框                    │
└──────────────┴──────────────────────────┘
```

### Android 会话布局

Android 建议单栈导航：

```text
会话列表页 -> 会话详情页
```

会话详情页：

- 顶部返回按钮 + 会话标题
- 中间消息列表
- 底部固定输入栏
- 避免复杂多栏和大弹窗

## API 客户端设计

多端客户端不要继续写死：

```js
baseURL: '/api'
```

而是统一通过配置生成：

```js
function normalizeServerUrl(url) {
  return String(url || '').replace(/\/+$/, '')
}

function apiBaseUrl(serverUrl) {
  return `${normalizeServerUrl(serverUrl)}/api`
}
```

axios：

```js
const service = axios.create({
  baseURL: apiBaseUrl(serverUrl),
  timeout: 7500000
})
```

socket：

```js
const socket = io(serverUrl, {
  transports: ['websocket', 'polling'],
  reconnection: true
})
```

文件和上传地址后续统一使用：

```js
function backendUrl(path) {
  return `${normalizeServerUrl(serverUrl)}${path}`
}
```

## 路由方案

多端客户端建议使用 hash 路由：

```js
new Router({
  mode: 'hash',
  routes
})
```

原因：

- Electron 静态文件加载更稳。
- Capacitor Android WebView 刷新和返回更简单。
- 不影响现有 Web 前端的 history 路由。

## 构建与运行

### Electron

开发：

```bash
cd clients/desktop
npm install
npm run dev
```

打包：

```bash
npm run build
```

输出：

```text
.exe
```

### Android

开发：

```bash
cd clients/android
npm install
npm run build
npx cap sync android
npx cap open android
```

真机测试：

1. 手机和后端在同一局域网。
2. 后端运行在 `0.0.0.0:5002`。
3. 手机 App 配置 `http://后端机器局域网IP:5002`。
4. 点击测试连接。
5. 登录并进入会话。

## 分阶段实施计划

### 阶段 1：基础多端骨架

- 新增 `clients/desktop`
- 新增 `clients/android`
- 建立统一 API client
- 建立服务器地址配置能力
- 建立 hash 路由
- 实现登录页
- 实现注册页

交付标准：

- 桌面端可以配置后端地址并登录。
- Android 端可以配置局域网后端地址并登录。

当前状态：已完成。

已新增：

```text
clients/
  README.md
  desktop/
    electron/
    src/
  android/
    capacitor.config.ts
    src/
```

阶段 1 已具备：

- Electron 桌面端工程骨架
- Capacitor Android 工程骨架
- hash 路由
- 后端地址配置页
- 后端健康检查
- 登录页
- 注册页
- token 本地保存
- 会话页占位骨架

运行桌面端：

```bash
cd clients/desktop
npm install
npm run dev
```

运行 Android 端：

```bash
cd clients/android
npm install
npm run build
npx cap add android
npx cap sync android
npx cap open android
```

局域网测试时，客户端服务器地址填写后端机器的局域网地址，例如：

```text
http://192.168.1.100:5002
```

### 阶段 2：会话列表与会话详情

- 实现会话列表
- 实现新建会话
- 实现消息列表
- 实现发送消息
- 实现基础 loading / error 状态

交付标准：

- 桌面端和 Android 端可以进入会话并发送消息。

### 阶段 3：实时消息

- 接入 socket.io
- 登录后建立连接
- 进入会话后 join room
- 离开会话后 leave room
- 接收 `new_message`
- 接收 Agent 状态事件

交付标准：

- 多端可以实时看到新消息和 Agent 响应状态。

当前状态：桌面端已完成，Android 暂缓。

桌面端已新增：

- `clients/desktop/src/services/socket.js`
- 会话页 socket 连接、join/leave room
- `new_message`
- `conversation_message_created`
- `conversation_message_delta`
- `conversation_message_status`
- 我的 Agent 页面
- 个人设置页面

桌面端新增页面：

```text
clients/desktop/src/views/Agents.vue
clients/desktop/src/views/Settings.vue
```

### 阶段 4：体验补齐

- Android 返回键处理
- token 过期处理
- 服务器地址切换
- 退出登录
- 网络异常提示
- 桌面端窗口尺寸和最小宽高
- Android 安全区和键盘遮挡处理

交付标准：

- 局域网环境下可稳定演示登录、注册、会话收发。

### 阶段 5：公网部署适配

- 后端部署公网 HTTPS
- 客户端默认服务器地址切换为公网地址
- Android 关闭或收紧明文 HTTP
- 检查 CORS 和 SocketIO origins
- 准备生产构建配置

交付标准：

- 桌面端和 Android 端可以连接公网后端。

## 风险与注意事项

### Android 访问 localhost

Android 真机不能使用 `localhost` 访问电脑后端。必须使用局域网 IP 或公网地址。

### Socket 连接

当前 Web 端使用 `io('/')`，多端必须改为连接用户配置的 `serverUrl`。

### 文件预览和下载

当前 Web 端大量使用 `/api/sandbox/...` 相对路径。多端首期不做 sandbox 文件预览，避免提前引入复杂 URL 兼容问题。

### HTTP 与 HTTPS

局域网测试可以使用 HTTP。生产环境建议 HTTPS。

### Web 前端隔离

现有 `frontend` 暂不改页面。多端客户端独立演进，等登录、注册、会话链路稳定后，再评估是否抽取公共组件或公共 API 包。

## 推荐最终路线

当前建议按以下路线执行：

1. 保留现有 Web 前端。
2. 新建 Electron 桌面客户端。
3. 新建 Capacitor Android 客户端。
4. 两端先实现独立的登录、注册、会话页面。
5. 两端共用同一套后端接口协议。
6. 后端地址由用户在客户端配置。
7. 局域网测试使用 `http://局域网IP:5002`。
8. 后端公网部署后切换为 HTTPS 地址。

这样可以最快建立多端能力，同时避免现有 Web 前端被多端适配逻辑污染。
