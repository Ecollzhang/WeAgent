# 项目管理模块 — 详细设计方案

> 文档版本: v1.0  
> 创建日期: 2026-07-29  
> 状态: 待确认

---

## 一、当前状态 vs 目标状态

| 维度 | 当前 | 目标 |
|------|------|------|
| 项目 | 仅名称/描述/技术栈/状态 | 增加成员、封面、编码规范、可见性等 |
| 迭代 | demoData 中仅有基础字段 | 完整 CRUD + 手动进度 + 统计 |
| 需求 | demoData + 弹窗查看，单负责人 | 独立详情页 + 多开发者 + 评论系统 + 子需求拆分 |
| Bug | demoData + 弹窗查看，单负责人 | 独立详情页 + 多开发者 + 评论系统 + 附件 |
| 甘特图 | 无 | 新增：时间轴可视化迭代/需求排期 |
| 评论系统 | 无 | 需求/Bug/迭代 均支持评论和嵌套回复 |
| 活动日志 | 无 | 记录所有变更操作 |

---

## 二、数据库模型设计

### 2.1 项目表 `rd_projects`（在现有基础上扩展）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| workspace_id | String(36) | 所属工作空间 |
| user_id | String(36) | 创建者 |
| name | String(200) | 项目名称 |
| description | Text | 项目描述 |
| cover_url | String(500) | **新增** — 封面图 URL |
| tech_stack | JSON | 技术栈 `{"frontend":"Vue","backend":"Flask","database":"MySQL","deployment":"Docker"}` |
| coding_standards | Text | **新增** — 编码规范/团队约定 |
| status | Enum | `active` \| `archived` |
| visibility | Enum | **新增** — `private` \| `team` \| `public`，默认 `team` |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

### 2.2 项目成员表 `rd_project_members`（新增）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| project_id | String(36) | 项目 ID (FK → rd_projects) |
| user_id | String(36) | 用户 ID |
| role | Enum | `owner` \| `admin` \| `developer` \| `viewer` |
| joined_at | DateTime | 加入时间 |

### 2.3 迭代表 `rd_iterations`（新增）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| project_id | String(36) | 项目 ID (FK → rd_projects) |
| name | String(200) | 迭代名称，如 "Sprint 1" |
| goal | Text | 迭代目标 |
| start_date | Date | 开始日期 |
| end_date | Date | 结束日期 |
| status | Enum | `planning` \| `active` \| `completed` \| `cancelled` |
| sort_order | Integer | 排序序号 |
| progress_manual | Integer(0-100) | **新增** — 手动设置的进度百分比 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

**进度计算规则：**
- 需求/Bug 完成百分比自动统计为 `progress_auto`
- `progress_manual` 优先级高于 `progress_auto`，允许 PM 手动修正
- 当 `progress_manual` 为 null 时，使用 `progress_auto`

### 2.4 需求表 `rd_requirements`（扩展）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| project_id | String(36) | 项目 ID (FK → rd_projects) |
| iteration_id | String(36) | 所属迭代 (FK → rd_iterations, nullable) |
| parent_id | String(36) | **新增** — 父需求 ID (FK → self, nullable)，支持子需求拆分 |
| title | String(300) | 需求标题 |
| description | Text | 需求描述（Markdown 格式） |
| acceptance_criteria | Text | **新增** — 验收标准（Markdown 格式） |
| priority | Enum | `p0` \| `p1` \| `p2` \| `p3` |
| status | Enum | `backlog` \| `todo` \| `in_progress` \| `in_review` \| `done` \| `closed` |
| type | Enum | **新增** — `feature` \| `enhancement` \| `bugfix` \| `tech_debt` \| `research` |
| story_points | Integer | 故事点 (0-100) |
| labels | JSON | 标签数组 `["前端","表单","用户体验"]` |
| start_date | Date | **新增** — 计划开始日期（用于甘特图） |
| due_date | Date | **新增** — 计划截止日期（用于甘特图） |
| completed_at | DateTime | **新增** — 实际完成时间 |
| created_by | String(36) | **新增** — 创建者 ID |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

**状态流转规则：**
```
backlog → todo → in_progress → in_review → done → closed
  ↑         ↑                    ↓           ↓
  └─────────┴────────────────────┴───────────┘  (可回退)
```

### 2.5 需求-开发者关联表 `rd_requirement_assignees`（新增，支持多开发者）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| requirement_id | String(36) | 需求 ID (FK → rd_requirements) |
| user_id | String(36) | 被指派的用户 ID |
| role | Enum | `primary` \| `reviewer` \| `tester`（区分职责） |
| assigned_at | DateTime | 指派时间 |

> **说明：** 一个需求可以有多个开发者，每个开发者有不同的角色。例如：张伟为主开发 (primary)，李娜为审查人 (reviewer)，陈静为测试 (tester)。

### 2.6 Bug 表 `rd_bugs`（扩展）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| project_id | String(36) | 项目 ID (FK → rd_projects) |
| iteration_id | String(36) | 所属迭代 (FK → rd_iterations, nullable) |
| requirement_id | String(36) | **新增** — 关联需求 (FK → rd_requirements, nullable) |
| title | String(300) | Bug 标题 |
| description | Text | 复现步骤 |
| expected_behavior | Text | 期望行为 |
| actual_behavior | Text | 实际行为 |
| severity | Enum | `blocker` \| `critical` \| `major` \| `minor` \| `trivial` |
| priority | Enum | `p0` \| `p1` \| `p2` \| `p3` |
| status | Enum | `open` \| `confirmed` \| `in_progress` \| `fixed` \| `verified` \| `closed` \| `wont_fix` |
| environment | String(200) | 运行环境描述 |
| browser_info | String(200) | **新增** — 浏览器信息 |
| os_info | String(200) | **新增** — 操作系统信息 |
| attachments | JSON | **新增** — 附件 URL 数组 `["https://xxx/screenshot1.png"]` |
| labels | JSON | 标签数组 |
| created_by | String(36) | **新增** — 提交者 ID |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |
| fixed_at | DateTime | **新增** — 修复时间 |
| verified_at | DateTime | **新增** — 验证时间 |

**状态流转规则：**
```
open → confirmed → in_progress → fixed → verified → closed
  ↓                   ↓            ↓
  └──→ wont_fix ←─────┴────────────┘
```

### 2.7 Bug-开发者关联表 `rd_bug_assignees`（新增，支持多开发者）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| bug_id | String(36) | Bug ID (FK → rd_bugs) |
| user_id | String(36) | 被指派的用户 ID |
| role | Enum | `fixer` \| `reviewer` \| `tester` |
| assigned_at | DateTime | 指派时间 |

### 2.8 评论表 `rd_comments`（新增，统一评论系统）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| project_id | String(36) | 项目 ID (FK → rd_projects) |
| target_type | Enum | `requirement` \| `bug` \| `iteration` |
| target_id | String(36) | 目标 ID |
| parent_id | String(36) | 父评论 ID (FK → self, nullable)，支持嵌套回复 |
| content | Text | 评论内容（Markdown 格式） |
| content_type | Enum | `text` \| `markdown` \| `system`（系统自动生成的状态变更记录） |
| author_id | String(36) | 作者 ID |
| is_pinned | Boolean | **新增** — 是否置顶 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |
| deleted_at | DateTime | 软删除时间 (nullable) |

> **说明：** 评论支持 Markdown 格式，`content_type=system` 的记录由系统自动创建（如 "张伟 将状态改为进行中"），不可编辑删除。

### 2.9 活动日志表 `rd_activity_logs`（新增）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| project_id | String(36) | 项目 ID (FK → rd_projects) |
| target_type | Enum | `requirement` \| `bug` \| `iteration` \| `project` |
| target_id | String(36) | 目标 ID |
| action | Enum | `created` \| `updated` \| `status_changed` \| `assigned` \| `commented` \| `deleted` |
| actor_id | String(36) | 操作者 ID |
| old_value | JSON | 变更前值 (nullable) |
| new_value | JSON | 变更后值 (nullable) |
| created_at | DateTime | 操作时间 |

---

## 三、API 设计

### 3.1 项目 (Project)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/rd/projects` | 项目列表 |
| POST | `/api/rd/projects` | 创建项目 |
| GET | `/api/rd/projects/:id` | 项目详情（含成员、统计） |
| PUT | `/api/rd/projects/:id` | 更新项目 |
| DELETE | `/api/rd/projects/:id` | 归档项目（软删除） |
| GET | `/api/rd/projects/:id/members` | 项目成员列表 |
| POST | `/api/rd/projects/:id/members` | 添加成员 |
| PUT | `/api/rd/projects/:id/members/:uid` | 修改成员角色 |
| DELETE | `/api/rd/projects/:id/members/:uid` | 移除成员 |
| GET | `/api/rd/projects/:id/context` | 项目上下文（技术栈+规范+文件结构） |

### 3.2 迭代 (Iteration)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/rd/projects/:id/iterations` | 迭代列表（含需求/Bug 统计数） |
| POST | `/api/rd/projects/:id/iterations` | 创建迭代 |
| GET | `/api/rd/projects/:id/iterations/:iid` | 迭代详情 |
| PUT | `/api/rd/projects/:id/iterations/:iid` | 更新迭代（含进度） |
| DELETE | `/api/rd/projects/:id/iterations/:iid` | 删除迭代 |
| PATCH | `/api/rd/projects/:id/iterations/:iid/status` | 切换迭代状态 |

### 3.3 需求 (Requirement)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/rd/projects/:id/requirements` | 需求列表（支持筛选：迭代/优先级/状态/负责人/类型/标签） |
| POST | `/api/rd/projects/:id/requirements` | 创建需求 |
| GET | `/api/rd/projects/:id/requirements/:rid` | 需求详情（含开发者、评论、子需求、活动日志） |
| PUT | `/api/rd/projects/:id/requirements/:rid` | 更新需求 |
| DELETE | `/api/rd/projects/:id/requirements/:rid` | 删除需求（软删除） |
| PATCH | `/api/rd/projects/:id/requirements/:rid/status` | 快速切换状态 |
| POST | `/api/rd/projects/:id/requirements/:rid/assignees` | 添加开发者 |
| DELETE | `/api/rd/projects/:id/requirements/:rid/assignees/:uid` | 移除开发者 |
| POST | `/api/rd/projects/:id/requirements/:rid/subtasks` | 创建子需求 |

### 3.4 Bug

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/rd/projects/:id/bugs` | Bug 列表（支持筛选：迭代/严重程度/优先级/状态/负责人/标签） |
| POST | `/api/rd/projects/:id/bugs` | 提交 Bug |
| GET | `/api/rd/projects/:id/bugs/:bid` | Bug 详情（含修复人、评论、活动日志） |
| PUT | `/api/rd/projects/:id/bugs/:bid` | 更新 Bug |
| DELETE | `/api/rd/projects/:id/bugs/:bid` | 删除 Bug |
| PATCH | `/api/rd/projects/:id/bugs/:bid/status` | 快速切换状态 |
| POST | `/api/rd/projects/:id/bugs/:bid/assignees` | 添加修复人 |
| DELETE | `/api/rd/projects/:id/bugs/:bid/assignees/:uid` | 移除修复人 |

### 3.5 评论 (Comment)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/rd/projects/:id/comments` | 评论列表（按 `target_type` + `target_id` 筛选） |
| POST | `/api/rd/projects/:id/comments` | 添加评论 |
| PUT | `/api/rd/projects/:id/comments/:cid` | 编辑评论 |
| DELETE | `/api/rd/projects/:id/comments/:cid` | 删除评论（软删除） |

### 3.6 活动日志 (Activity Log)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/rd/projects/:id/activities` | 项目活动日志（分页，按时间倒序） |
| GET | `/api/rd/projects/:id/activities?target_type=requirement&target_id=xxx` | 指定对象的活动日志 |

### 3.7 甘特图数据

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/rd/projects/:id/gantt` | 获取甘特图数据（迭代+需求的时间线） |

---

## 四、前端路由和页面结构

```
/projects                              → RdProjects.vue             (项目列表，已有)
/projects/:id                          → RdProjectDetail.vue         (项目仪表盘，已有，保留)
/projects/:id/requirements             → RdRequirementList.vue       (新增：需求独立列表页)
/projects/:id/requirements/:rid        → RdRequirementDetail.vue     (新增：需求详情页)
/projects/:id/bugs                     → RdBugList.vue               (新增：Bug 独立列表页)
/projects/:id/bugs/:bid                → RdBugDetail.vue             (新增：Bug 详情页)
/projects/:id/gantt                    → RdGanttChart.vue            (新增：甘特图页面)
```

**页面关系说明：**
- 现有项目详情页的 Tab（概览/迭代/需求/Bug/代码仓库）**保留**
- Tab 中的需求和 Bug 列表点击后，**跳转到独立详情页** `/projects/:id/requirements/:rid`
- 侧边栏「项目管理」入口 → 项目列表 → 项目详情（Tab 概览）
- 侧边栏可考虑新增「甘特图」快捷入口

---

## 五、前端页面详细设计

### 5.1 需求详情页 `RdRequirementDetail.vue`

**布局：** 左右两栏布局

**左侧主内容区（占 70%）：**
- 标题（H2）+ 编辑按钮（仅创建者/负责人可见）
- 状态流转按钮组（当前状态高亮，可点击下一步状态）
- 需求描述（Markdown 渲染区域）
- 验收标准卡片（Markdown 渲染，绿色边框）
- 子需求列表（可折叠表格）
- ── 分隔线 ──
- 评论区域：
  - 评论总数统计
  - Markdown 编辑器 + 提交按钮（支持工具栏：加粗、代码块、图片链接、@提及）
  - 评论列表（每条评论：头像、姓名、时间戳、Markdown 渲染内容、回复/编辑/删除按钮）
  - 支持嵌套回复（最多 2 层）

**右侧信息面板（占 30%，固定吸顶）：**
- 状态（Select 下拉可切换）
- 优先级（Select 下拉可切换）
- 类型（Select 下拉）
- 开发者列表（多选 Tag 输入框，支持添加/移除，区分角色标签）
- 故事点（数字输入）
- 所属迭代（Select 下拉）
- 计划时间（开始日期 → 截止日期 DatePicker）
- 标签（Tag 输入，支持新增/删除）
- 关联分支列表（自动关联，显示分支名 + 状态 Tag）
- 活动日志时间线（最近 20 条，滚动加载更多）

### 5.2 Bug 详情页 `RdBugDetail.vue`

**布局：** 与需求详情页相同的左右两栏布局

**左侧主内容区（占 70%）：**
- 标题 + 编辑按钮
- 状态流转按钮组
- 复现步骤（Markdown 渲染）
- 期望行为（绿色卡片背景）
- 实际行为（红色卡片背景）
- 附件预览区（图片缩略图网格，点击放大预览）
- ── 分隔线 ──
- 评论区域（与需求相同）

**右侧信息面板（占 30%）：**
- 状态（Select 下拉：open → confirmed → in_progress → fixed → verified → closed → wont_fix）
- 严重程度（可视化的彩色标签）
- 优先级（Select）
- 修复人列表（多选 Tag，区分 fixer/reviewer/tester）
- 关联需求（Select 搜索选择）
- 所属迭代（Select）
- 环境信息组：
  - 运行环境（Input）
  - 操作系统（Select: Windows/macOS/Linux/iOS/Android）
  - 浏览器（Select: Chrome/Firefox/Safari/Edge）
- 标签（多选 Tag）
- 活动日志时间线

### 5.3 甘特图 `RdGanttChart.vue`

**功能：**
- 横轴为日期时间线，纵轴为迭代+需求列表
- 树形结构：迭代（粗条）→ 子项需求（细条）
- 颜色编码：
  - 迭代：灰色（planning）/ 蓝色（active）/ 绿色（completed）
  - 需求：按状态着色
  - Bug：红色系
- 今日线：红色竖线标注
- 交互：
  - 拖拽调整需求起止日期
  - 点击需求条跳转到详情页
  - 支持缩放：周/月/季度视图
  - 支持展开/收起迭代

**实现方案：**
- 建议使用 `echarts` 的甘特图插件或 `dhtmlx-gantt`（轻量版）
- 或者基于原生 DOM/CSS Grid 手写甘特图组件（灵活度更高）

**顶部工具栏：**
- 视图切换（日/周/月）
- 日期范围选择器
- 筛选：迭代、负责人、状态
- 导出图片按钮（html2canvas）

### 5.4 更新项目详情页 `RdProjectDetail.vue`

**新增 Tab：甘特图 Tab**
- 概览 | 迭代 | 需求 | Bug | 甘特图 | 代码仓库
- 甘特图 Tab 内嵌简化版甘特图（可点击 "全屏查看" 跳转到独立页面）

**概览 Tab 更新：**
- 新增「团队效率」面板：需求吞吐量、Bug 修复率、迭代燃尽图
- 迭代进度条旁增加 "手动调整进度" 滑块

---

## 六、前端组件树

```
src/views/rd/
├── RdProjects.vue               # 项目列表（已有）
├── RdProjectDetail.vue          # 项目仪表盘（已有，更新）
├── RdRequirementList.vue        # 需求列表页（新增）
├── RdRequirementDetail.vue      # 需求详情页（新增）
├── RdBugList.vue                # Bug 列表页（新增）
├── RdBugDetail.vue              # Bug 详情页（新增）
├── RdGanttChart.vue             # 甘特图（新增）
├── RdRepos.vue                  # 仓库列表（已有）
├── RdRepoDetail.vue             # 仓库详情（已有）
├── RdReviews.vue                # 代码审查（已有）
├── RdBuilds.vue                 # 构建管理（已有）
└── demoData.js                  # 演示数据（已有，大幅扩展）

src/components/rd/
├── CreateProjectDialog.vue      # 创建项目弹窗（已有）
├── CommentEditor.vue            # Markdown 评论编辑器（新增）
├── CommentList.vue              # 评论列表组件（新增）
├── AssigneeSelector.vue         # 多开发者选择器（新增）
├── ActivityTimeline.vue         # 活动日志时间线（新增）
├── StatusFlowBar.vue            # 状态流转按钮组（新增）
├── GanttChart.vue               # 甘特图核心组件（新增）
└── RequirementCard.vue          # 需求卡片（新增，在甘特图/看板中复用）
```

---

## 七、演示数据扩展计划

`demoData.js` 需扩展的内容：

1. **项目成员** — 每个项目 3-5 个成员，不同角色
2. **需求的验收标准** — 每个需求 2-4 条验收标准（列表格式）
3. **多开发者需求** — 至少 2 个需求有 2 个以上开发者
4. **需求评论** — 至少 3 个需求有评论（2-4 条评论+回复）
5. **Bug 评论** — 至少 2 个 Bug 有评论
6. **Bug 附件** — 至少 1 个 Bug 有截图附件
7. **子需求** — 至少 1 个需求拆分了 2-3 个子需求
8. **活动日志** — 每个需求/Bug 有 5-10 条操作记录
9. **甘特图数据** — 所有迭代和需求的起止日期

---

## 八、需要确认的问题

| # | 问题 | 建议方案 | 影响范围 |
|---|------|----------|----------|
| 1 | **用户体系** — 目前成员用 `user_id` 关联，但现有系统是否有完整用户表？还是先用 demo 开发者列表？ | 建议先用 demo 开发者数据（张伟、李娜、王磊、陈静），后续接入真实用户体系 | 成员表、指派、评论 |
| 2 | **需求详情页访问方式** — 是跳转独立路由还是项目详情页内全屏弹窗？ | **建议独立路由** `/projects/:id/requirements/:rid`，URL 可分享、浏览器前进后退友好 | 路由、页面结构 |
| 3 | **甘特图位置** — 独立页面还是项目详情页的 Tab？ | **建议两者都有**：独立路由 `/projects/:id/gantt` + 概览 Tab 放缩略版 | 路由、组件复用 |
| 4 | **子需求嵌套层级** — 支持几级嵌套？父子进度是否联动？ | 建议先支持**一级拆分**（父子），后续可扩展。子需求全部完成 → 父需求自动 "done" | 需求表、详情页 |
| 5 | **附件上传** — Bug 的截图/文件是否需要对接文件存储？ | Demo 阶段先用**外链 URL 占位**，后续对接 MinIO 或本地存储 | Bug 表、详情页 |
| 6 | **通知系统** — 状态变更和指派时是否需要站内/邮件通知？ | 建议先记录在**活动日志**中，通知作为后续迭代 | 活动日志 |
| 7 | **权限粒度** — `owner/admin/developer/viewer` 四角色权限矩阵 | 建议先实现基本读写：owner/admin 可管理成员和删除，developer 可编辑需求和 Bug，viewer 只读 | 成员管理、API 鉴权 |
| 8 | **后端实现优先级** — 模型/API 和前端页面是否同时实现？ | 建议**先做前端页面 + 完整 Demo 数据**，后端 API 后续分批对接。前端 Demo 数据结构尽量贴近真实 API 返回 | 开发节奏 |

---

## 九、实现建议分期

### Phase 1：前端骨架 + Demo 数据（当前阶段）
- [ ] 扩展 `demoData.js`（成员、评论、活动日志、甘特图数据等）
- [ ] 新建 `RdRequirementDetail.vue`（需求详情页）
- [ ] 新建 `RdBugDetail.vue`（Bug 详情页）
- [ ] 新建 `RdGanttChart.vue`（甘特图）
- [ ] 新建评论相关组件（CommentEditor, CommentList）
- [ ] 新建通用组件（AssigneeSelector, ActivityTimeline, StatusFlowBar）
- [ ] 更新 `RdProjectDetail.vue`（新增甘特图 Tab，概览面板增强）
- [ ] 更新路由配置

### Phase 2：后端模型 + API
- [ ] 新增数据库模型（迭代/需求/Bug/成员/评论/活动日志）
- [ ] 实现 CRUD 控制器 + Service 层
- [ ] 新增 `/api/rd/spec` 中的端点描述
- [ ] API 鉴权和权限校验

### Phase 3：前后端联调 + 细节打磨
- [ ] 前端切换数据源从 Demo → API
- [ ] 甘特图拖拽交互
- [ ] Markdown 编辑器集成
- [ ] 附件上传功能
- [ ] 性能优化（虚拟滚动、分页加载）
